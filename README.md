#  🧮 Calculator 만들기 🧮

> ### 프로젝트 소개
- 기본적인 사칙연산(+,-,*,/)을 수행하는 계산기 구현
- 사용자로부터 숫자와 연산자를 입력받아 결과 출력
- 연산 결과는 리스트에 저장하여 누적 관리

> ### 피드백 반영
0) 피드백 반영 TIL 작성 (아래 링크)
 - https://velog.io/@gpekd5/TIL-Calculator-%EA%B3%BC%EC%A0%9C-%ED%94%BC%EB%93%9C%EB%B0%B1-%EB%B0%98%EC%98%81
1) Enum 클래스 내부로 계산 로직 이동 26.03.27
 - enum 클래스 학습 및 정리 (아래 링크)
 - https://velog.io/@gpekd5/Java-%EB%AC%B8%EB%B2%95-enum-%EC%9D%B4%ED%95%B4%ED%95%98%EA%B8%B0
2) ArrayList가 아닌 List 타입으로 선언
3) 제네릭 설계 재구현
 - Main 메서드
   - 입력 단계부터 double로 고정하는 것이 아니라 Number 을 통해 타입을 유지하도록 변경
 - ArithmeticCalculator.java
   - 여러 제네릭 타입을 사용하여 다른 타입을 가지도록 구현
   - 타입에 따라 연산을 분기하여 입력값이 모두 정수인 경우에는 정수 연산을 수행하고 이외에는 실수 연산
   - 결과는 Number 타입으로 선언하여 정수와 실수 결과를 모두 담기
 - OperatorType.java
   - 메서드 오버로딩하여 같은 calculate 메서드를 int용 / double용으로 나눠서 처리
   - 입력받아 타입을 구분하는 방식으로 수정

> ### STEP1. 클래스 없이 기본적인 연산 수행 계산기 구현
 
### 주요 기능
0) 기본 클래스 내 main 메서드에서만 구현 
1) 첫번째 숫자 입력 (0이상의 정수가 아니면 재입력 요청)
2) 두번째 숫자 입력 (0이상의 정수가 아니면 재입력 요청)
3) 사칙연산 입력 (+, -, *, / 가 아니면 재입력 요청)
4) "exit" 입력을 하면 프로그램 종료, 그렇지 않으면 1~3 반복 수행


> ### STEP2. 클래스 적용해 기본적인 연산 수행

### 주요기능
1. 전체적인 기능 및 흐름은 STEP1 과 동일
2. 사칙연산을 별도의 Calculator라는 Class를 생성하여 구현
3. 사칙연산 결과값을 resultList 컬렉션(ArrayList)에 할당하여 표시
4. 결과 리스트에 강제로 [1,2,3,4,5] 데이터를 입력하는 기능 구현
5. 가장 먼저 저장된 데이터 삭제 기능 구현

> ### STEP3. Enum, 제네릭, 람다&스트림 이해한 계산기 구현

### 주요기능
1. EnumClass 생성 후 연산 메서드에서 Enum 객체를 받도록 구현
2. ArithmeticCalculator 클래스를 제네릭 타입으로 수정하여 double 연산이 가능하도록 전체적인 수정
3. 2번 요구사항에 따라 양의 정수와 관련된 예외처리 제거
4. 저장된 연산 결과들 중 스캐너로 입력받은 값보다 큰 결과값을 데이터처리하여 출력 구현

